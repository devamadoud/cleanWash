{% extends 'fullyAuthLayout.html.twig' %}

{% form_theme form 'tailwind_2_layout.html.twig' %}

{% block title %}
  Modifier client
{% endblock %}

{% block body %}
  <div class="customer-form items-center flex min-h-full justify-center my-auto mx-auto lg:px-8">
    <div class="px-4 w-max mx-auto mt-12 justify-center md:px-8">
      <div class="items-start justify-between md:flex border-b pb-6 text-gray-600">
        <div class="max-w-lg">
          <h3 class="text-gray-800 text-xl font-bold sm:text-2xl">Modifier les informations d'un client</h3>
          <p class="text-gray-600 mt-2">Modifier les informations d'un client, verifiez que les informations sont precises et que l'adresse du client est assez claire !</p>
        </div>
        <div class="mt-3 md:mt-0">
          <a href="{{ path('customer.index') }}" class="inline-flex border border-gray-300 items-center justify-center px-5 py-3 mr-3 text-base font-medium text-center text-purple-600 rounded-lg bg-gray-100 hover:bg-gray-200 focus:ring-4 focus:ring-gray-300 dark:focus:ring-gray-900" rel="nofollow">Retour</a>
        </div>
      </div>
      {{ include('customer/_form.html.twig', { button_label: 'Mettre à jour' }) }}

      {% if app.user.shop %}
        {{ include('customer/_delete_form.html.twig') }}
      {% endif %}
    </div>
  </div>
{% endblock %}
