{% extends 'base.html.twig' %}

{% block title %}
  Nouveau client
{% endblock %}

{% form_theme form 'tailwind_2_layout.html.twig' %}

{% block body %}
  <div class="customer-form items-center flex min-h-full justify-center my-auto mx-auto lg:px-8">
    <div class="px-4 w-max mx-auto mt-12 justify-center md:px-8">
      <div class="items-start justify-between md:flex pb-6">
        {% if app.user %}  
          <div class="max-w-lg">
            <h3 class="text-gray-800 text-xl font-bold sm:text-2xl">Nouveau client ?</h3>
            <p class="text-gray-600 mt-2">Gestion de vos employées, ajoutez, suspendez ou révokez un membre de votre équipe.</p>
          </div>
          <div class="mt-3 md:mt-0">
            <a href="{{ path('customer.index') }}" title="Retour à la liste des clients" class="inline-flex items-center border border-gray-300 justify-center px-5 py-3 mr-3 text-base font-medium text-center text-purple-600 rounded-lg bg-gray-100 hover:bg-gray-200 focus:ring-4 focus:ring-gray-300 dark:focus:ring-gray-900">Retour</a>
          </div>

          {% else %}
          <div class="max-w-lg">
            <h3 class="text-purple-800 text-xl font-bold sm:text-2xl">Nouveau client ?</h3>
            <p class="text-gray-800 text-sm font-medium mt-2">C'est votre première fois sur la plateform ? pas de panique c'est très simple, veuillez autoriser la localisation en cliquant sur le button localisez-moi, de se fait ça vas permettre a notre collecteur &amp; livreur de vous trouver plus facilement pour la collecte et la livraison.</p>
          </div>
          <div class="mt-3 md:mt-0">
            <a href="{{ path('customer.index') }}" title="Retour à la liste des clients" class="inline-flex items-center border border-gray-300 justify-center px-5 py-3 mr-3 text-base font-medium text-center text-purple-600 rounded-lg bg-gray-100 hover:bg-gray-200 focus:ring-4 focus:ring-gray-300 dark:focus:ring-gray-900">Retour</a>
          </div>
        {% endif %}
      </div>
      {{ include('customer/_form.html.twig') }}
    </div>
  </div>
{% endblock %}
