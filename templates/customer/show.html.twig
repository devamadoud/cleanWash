{% extends 'fullyAuthLayout.html.twig' %}

{% block title %}Customer{% endblock %}

{% block body %}
    <h1>Customer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ customer.fullName }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ customer.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ customer.adress }}</td>
            </tr>
            <tr>
                <th>CoordLng</th>
                <td>{{ customer.coordLng }}</td>
            </tr>
            <tr>
                <th>CoordLat</th>
                <td>{{ customer.coordLat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('customer.index') }}">back to list</a>

    <a href="{{ path('customer.edit', {'id': customer.id}) }}">edit</a>

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
